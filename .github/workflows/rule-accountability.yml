name: rule-accountability

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  rule-accountability:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Run accountability CI script
        run: ./scripts/ci/run.sh accountability --channel stable
